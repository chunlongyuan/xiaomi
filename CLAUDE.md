# 小米学习乐园 — 项目约定

面向 4-6 岁儿童的数学 / 语文 / 英语练习网页。纯静态，无构建步骤，
GitHub Actions 部署到 GitHub Pages。

## 用户明确要求的产品规则（不要违反）

### 答题反馈
- **答题过程中不要弹 toast**。答对答错都只用视觉特效 + 音效 + 振动：
  - 答对：emoji burst + 撒花 + 上行三连音 + 双击式振动
  - 答错：选项抖动变红 + 下降双音 + 长振动
- 不要出现 "你真棒" / "再想想" 之类的文字弹窗。

### 选项卡不能泄露答案
- 选项按钮**只显示 label，绝不显示 value 文字**。
  之前 `<div class="cap">${value}</div>` 把答案（如"水果"）
  直接写在了 emoji 下面，孩子不看题也能选对。
- 看图选词 / 看词选图 / phonics 等题型，选项一律只出图或只出词。

### 游戏必须有输的设计
- 所有奖励小游戏都要有失败条件，不能只赢不输。
- 戳气球有炸弹 💣，戳中直接失败。
- 反应类游戏点错 / 连续漏掉会扣分。
- **失败后不给「再来一次」**。玩游戏是完成练习的奖励，
  一局定输赢才有分量。失败弹窗只有「继续 →」。

### 统一翻牌交互
- 答题选项和游戏里的卡片，点击都用同一套 `.flipcard` 翻转动画：
  `.fc-front` / `.fc-back` 两面 + `.flipped` 翻转 + `.fc-ok` / `.fc-bad` 着色。
- 记忆翻牌、找不同、数字排队、猜谜语的卡片都复用这套。

### 设置页
- 每个学科的每个分级、每个奖励游戏都能单独开关（`store.isEnabled`）。
- 关掉的内容不出现在分级页 / 奖励抽取 / 游戏厅里。
- 一个分组不允许全部关掉（至少留一项，否则页面空白）。

### 快捷入口
- **头像**单击 → 档案页。
- **名字**快速三击（500ms 内点满 3 下）→ 直接进游戏厅；
  慢速单击名字仍然是去档案页。

### 幼儿友好底线
- 无排行榜、无广告、无外链、无社交分享。
- 大按钮 ≥ 60px。
- 分数不减到负数。

## 分级依据
- 数学：教育部《3-6岁儿童学习与发展指南》+ 部编版一年级
- 语文：同上 + 部编版一年级上册识字表（随文识字/整词识字）
- 英语：幼儿英语启蒙 Phonics 教学法

## 技术约定
- 语音：`scripts/audio-phrases.json` 里的短语在 CI 用 edge-tts
  预生成 MP3（中文 XiaoyiNeural / 英文 JennyNeural），
  运行时优先播 MP3，找不到才回落浏览器 SpeechSynthesis。
- 每次部署 CI 会给所有 import/href/src 追加 `?v=<sha>` 破缓存。
- 存档在 localStorage，多小朋友档案独立。

## 开发
```bash
python3 -m http.server 8080    # 本地跑，别用 file:// 打开
node --check js/**/*.js         # 提交前语法检查
```
